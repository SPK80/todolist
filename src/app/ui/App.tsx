import React from 'react'
import { HashRouter } from 'react-router-dom'
import { AppContent } from './AppContent'

export const App = () => (
    <HashRouter>
        <AppContent />
    </HashRouter>
)
